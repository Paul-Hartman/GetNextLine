{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "gcc",
            "args": [
                "-g",
                "${workspaceFolder}/get_next_line.c",
                "${workspaceFolder}/get_next_line_utils.c",
                "-o",
                "${workspaceFolder}/get_next_line"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}